# Use this script to update the dbic classes.  This will fail with an error if
# any changes have been made above the checksum line, so DON'T do that!

./script/catalystx_jquery_tutorial_create.pl model PizzaStore DBIC::Schema PizzaStore::Schema create=static components=TimeStamp components=EncodedColumn dbi:mysql:pizzashop steve ss
